{
  "name": "winwright-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The documentation area, at /winwright/docs/. Its own npm project, building into the site's dist/docs.",
  "scripts": {
    "prebuild": "node ../scripts/product.mjs && node ../scripts/grammar.mjs && node ../scripts/format.mjs && node ../scripts/verbs.mjs && node ../scripts/project.mjs",
    "dev": "npm run prebuild && astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.9",
    "astro": "^7.2.9"
  }
}
