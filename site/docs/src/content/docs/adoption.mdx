---
title: One adoption, end to end
description: The refusal most adoptions meet before they run anything, the one line that repairs it, and a case running against a real window — all captured from real runs rather than written from memory.
---

import { Aside } from "@astrojs/starlight/components";

import Captured from "../../components/Captured.astro";
import Walkthrough from "../../components/Walkthrough.astro";
import adoption from "../../data/adoption.captured.json";
import walkthrough from "../../data/walkthrough.captured.json";

Everything below is what a real build and a real run printed. Both are captured by commands in
this repository rather than typed onto the page, so a message whose wording changed fails a
check here instead of misleading you halfway through.

## The shape that breaks

An adopting repository usually has the application's own project at the root and the project
that drives it underneath:

```
<your repository>/
  {adoption.shape.application}
  {adoption.shape.driving}
```

That is the natural place to put it, and it is the arrangement that fails. Every default glob
the SDK applies reaches the **whole tree below the project file**, so the driving project gets
compiled *into* the application.

Built on its own, the driving project is perfectly fine:

<Captured step={1} />

Then the application:

<Captured step={2} />

## Reading that

Nine errors, every one of them `CS0579`, every one naming a generated file, and not one of them
naming the folder that caused it. With `UseWPF` on, the project in the brackets can be a
`_wpftmp` project you never wrote — which reads like a WPF problem and sends you looking through
XAML.

It is not WPF and it is not the generated files. The driving project's own build wrote a set of
assembly attributes, the application's globs reached down and compiled them as well as its own,
and two copies of one attribute is what `CS0579` is.

<Aside type="caution" title="Why this arrives late, and about the wrong project">
The application builds cleanly until something has built the driving project — so it appears the
first time somebody runs the whole thing rather than when the folder was added, and the error
names the project that was fine.
</Aside>

## The one line that repairs it

In the **application's** project file, not the driving one:

```xml
{adoption.excludes}
```

<Captured step={3} />

`DefaultItemExcludes` and not a `Compile Remove`, because it is every glob and not only that
one: `Page`, `Resource` and `EmbeddedResource` reach down there too, and an application with
`UseWPF` on applies all of them.

<Aside title="This repository holds itself to that line">
`samples/Adopter` is an adoption kept inside this repository, with a driving project underneath
it for exactly this reason. The line above is read out of that project file when the capture is
taken, so it is the spelling this repository actually depends on rather than one written for a
page — take it out there and the sample is what fails.
</Aside>

## Then the run

With the build green there are two files to write and one command to run. This is
`samples/Walkthrough` in this repository — an application with a label and a button, a case
written against it, and the four calls an adopting project writes — captured on a desk of its
own.

The project declaration, which every relative path resolves against:

<Walkthrough file="declaration" />

The case. It reads the label, presses the button, and reads the label again:

<Walkthrough file="case" />

<Aside title="Why `reads` is `name` and not `text`">
The first capture of this run answered **Failed**, on both assertions, with `reads` set to
`text`. A label is not a text-editing control, so it offers no text pattern and the reading
answered nothing — and an expectation about a reading that was never there does not hold.

What a label says is its **name**. That is the sort of thing a page written from memory gets
wrong quietly; here it was a red run, and the fix is on the page because the failure was.
</Aside>

Building the two projects:

<Walkthrough step={1} />

<Walkthrough step={2} />

And running every case the sample declares:

<Walkthrough step={3} />

The first line is the claim and the rest is what it rests on. `Sentence()` states what did
**not** run before it states the outcome, because a pass over two of nine cases is a different
claim from a pass — and `VerdictSummary.Render` is what names an assertion that failed, or one
that never ran and the precondition that was absent.

## What was captured, and how to take it again

<p class="ww-figures">
  SDK <strong>{adoption.sdk}</strong> for the build, <strong>{walkthrough.sdk}</strong> for the
  run. {adoption.steps.length + walkthrough.steps.length} commands, exit codes{" "}
  {[...adoption.steps, ...walkthrough.steps].map((one) => one.code).join(", ")}.
</p>

`capture-adoption.cmd` builds the throwaway tree and rewrites the first half. It needs no desk
and no packaged feed, and it answers in seconds.

`capture-walkthrough.cmd` takes the second half, and it needs both: the case creates a real
window and reads it back, and the sample restores the engine from a packed feed the way an
adopting repository restores it from nuget.org. Run it the way this repository runs anything
that needs a desk — in a guest, through `run-tests-vm.cmd`.

## Where to go next

- [Installing it](/winwright/docs/installing/) — the package references, and the MSBuild line an
  application at a repository root needs.
- [Your first case](/winwright/docs/first-case/) — the same two files, field by field.
- [Reading the verdict](/winwright/docs/verdicts/) — the other refusal an adoption meets, which
  is a run that could not get the desk it needed.
