{
  "name": "winwright-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The public site for winwright — copy in one module, sections only render it.",
  "scripts": {
    "dev": "vite",
    "generate": "node scripts/product.mjs",
    "build": "npm run generate && tsc -b && vite build && node scripts/og-image.mjs && vite build --ssr src/entry-server.tsx --outDir dist-server && node scripts/prerender.mjs",
    "build:client": "vite build",
    "prerender": "vite build --ssr src/entry-server.tsx --outDir dist-server && node scripts/prerender.mjs",
    "typecheck": "tsc -b",
    "test": "node --test scripts/*.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@tailwindcss/vite": "^4.1.4",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^6.0.5",
    "node-html-parser": "^9.0.1",
    "tailwindcss": "^4.1.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
